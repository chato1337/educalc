# Nombre de la aplicación (login, rail, título del documento, etc.).
VITE_APP_NAME=INEAC — Docente

# URL base del backend (desde el navegador). Sin barra final.
# Vacío en dev = same-origin (proxy Vite /api → VITE_PROXY_API_TARGET).
# En build de producción debe coincidir con el API público.
VITE_API_BASE_URL=

# Destino del proxy Vite para /api (solo dev).
VITE_PROXY_API_TARGET=http://127.0.0.1:8000

# Opcional. Hosts extra permitidos por Vite en dev (reverse proxy / dominio público).
# Separados por coma. ineac.chatuzpark.store ya está incluido en vite.config.ts por defecto.
# VITE_ALLOWED_HOSTS=otro.ejemplo.com

# Face-Auth SSO: la app lee GET /api/auth/faceauth/config/ (sin api_key).
# Registrar en Face-Auth la redirect_uri exacta: http://localhost:8443/auth/callback
