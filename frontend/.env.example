# Opcional. Si está vacío o no existe, las peticiones van al mismo origen del front
# y en desarrollo Vite enruta `/api` al backend (ver vite.config.ts → server.proxy).
#
# VITE_API_BASE_URL=http://127.0.0.1:8000
